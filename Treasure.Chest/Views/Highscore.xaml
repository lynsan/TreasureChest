<Page x:Class="Treasure.Chest.Views.Highscore"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Treasure.Chest.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Highscore">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="6*"></RowDefinition>
        </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="1" Content="Highscore" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" ></Label>
        
        <Border  
             CornerRadius="0" 
             Background="White"
             Grid.Column="1"
             Grid.Row="1">
            
            
            <DataGrid Background="White" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="True">

                <DataGrid.Resources>
                    <Style TargetType="{x:Type DataGridColumnHeader}">
                        <Setter Property="Background" Value="SpringGreen"></Setter>
                        <Setter Property="FontWeight" Value="Bold"></Setter>
                        <Setter Property="BorderThickness" Value="1,0,1,2"></Setter>
                        <Setter Property="BorderBrush" Value="Green"></Setter>
                        <Setter Property="MinHeight" Value="20"></Setter>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Player Name" Width="2*"/>
                    <DataGridTextColumn  Header="Score" Width="1*"/>
                    <DataGridTextColumn  Header="Time" Width="2*"/>
                </DataGrid.Columns>
                
            </DataGrid>
        </Border>

    </Grid>
</Page>
